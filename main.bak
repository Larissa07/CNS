#include <16f876a.h>
#use delay(clock=16000000)
#include <lcd_modificado.c>
#include <Funçoes.c>
void main()
{
   char x,i;
   inicializa_lcd();
   port_b_pullups(true);
   while(1)
   {
   for(i=0;i<99;i++)x=read_eeprom(i);
  
   
   
   
   
   }
}
